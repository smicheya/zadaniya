<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Месяцы</title>
</head>
<body>

<form action="" method="post" class="calculate">
    <input type="text" name="day" class="days" placeholder="введите день">
    <input type="text" name="month" class="months" placeholder="введите месяц">

    <input class="submit_form" type="submit" name="submit" value="Вывести дату"> 
 </form>

<?php

$day = $_POST['day'];
$month = $_POST['month'];

$months = ['января', 'февраля', 'марта', 'апреля', 'мая', 'июня', 
    'июля', 'августа', 'сентября', 'октября', 'ноября', 'декабря'];

if ( ($month == 4 || $month == 6 || $month == 9 || $month == 11) && $day > 30) {
    echo 'Невозможная дата.';
    return;
}
if ($month == 2 && $day > 29) {
    echo 'Невозможная дата.';
    return;
}
if ($day > 31 || $day < 1 || $month > 12 || $month < 1) {
    echo 'Невозможная дата.';
    return;
}

$month_str = $months[$month - 1];

echo $day . ' ' . $month_str;

?>

</body>
</html>
